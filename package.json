{
  "name": "aws-events-adapter",
  "version": "0.1.6",
  "description": "NPM package to handle and dispatch AWS events to a Lambda function.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watchAll",
    "clean": "rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EEduardoG/aws-events-adapter.git"
  },
  "keywords": [
    "aws",
    "lambda",
    "eventbridge",
    "apigateway",
    "adapter"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "serverless-request-manager": "^0.0.9"
  }
}
